"""SIMUS-based simulation backends (e.g., PyMUST).

This package is intentionally isolated from detector/evaluation code: it only
produces canonical IQ cubes (Icube) and masks for downstream acceptance-bundle
generation.
"""

